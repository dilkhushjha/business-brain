"use client";
import {useEffect,useState} from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000/api";const BUSINESS_ID=process.env.NEXT_PUBLIC_BUSINESS_ID||"11111111-1111-1111-1111-111111111111";const money=(n:number)=>`₹${n.toLocaleString("en-IN",{maximumFractionDigits:0})}`;
type S={outstanding:number;overdue:number;overdue_pct:number;buckets:Record<string,number>};type C={name:string;overdue_amount:number;days_overdue:number};
export default function ReceivablesIntelligence(){const[s,setS]=useState<S|null>(null),[c,setC]=useState<C[]>([]);useEffect(()=>{Promise.all([fetch(`${API}/receivables/${BUSINESS_ID}/summary`).then(r=>r.ok?r.json():null),fetch(`${API}/receivables/${BUSINESS_ID}/overdue?limit=5`).then(r=>r.ok?r.json():[])]).then(([a,b])=>{setS(a);setC(b)}).catch(()=>{})},[]);if(!s||!s.outstanding)return null;return <section className="card"><div className="cardTitle"><span>◷</span><h3>Receivables & cash</h3><small>Outstanding invoices</small></div><div className="marginStats"><div><span>Outstanding</span><b>{money(s.outstanding)}</b></div><div><span>Overdue</span><b>{money(s.overdue)}</b></div><div><span>Overdue share</span><b>{s.overdue_pct}%</b></div></div>{c.length>0&&<div className="marginAlerts"><strong>Customers needing collection</strong>{c.map(x=><div className="marginRow" key={x.name}><span>{x.name}</span><b>{money(x.overdue_amount)}</b><em>{x.days_overdue} days overdue</em></div>)}</div>}</section>}
