# Data Flow

Tally/Excel/CSV → ingestion → validation → normalization → entity resolution → canonical model → semantic layer → metrics/signals/ML → evidence → agent → explanation.

Raw source files remain immutable and processing should be reproducible.
